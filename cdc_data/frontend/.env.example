# Base URL of the Disease Radar backend API.
# Copy this file to .env and adjust if your backend runs somewhere other
# than the default (e.g. a different port, or a deployed URL).
VITE_API_BASE=http://localhost:8000
